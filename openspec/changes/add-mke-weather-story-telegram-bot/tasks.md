## 1. Project and configuration foundation

- [x] 1.1 Create the application structure, Python 3.13/arm64-compatible dependency manifest, committed `uv.lock`, pinned `uv`/Python toolchain inputs, and local test tooling for the scheduled Weather Story service; require `uv sync --locked` for production dependency installation.
- [x] 1.2 Define a versioned all-NWS-office seed set and validated registry records with unique office ID, absolute HTTPS Weather Stories URL, NWS-seeded display name/address/telephone/email/office-home URL/region name/region-home URL, geocoded latitude/longitude, timezone lookup derived from those coordinates, Telegram channel identifier, and active flag; allow a channel only to be absent while inactive, require a unique channel before activation, and activate only MKX for MVP. Define validated environment-scoped non-secret channel/alert-recipient and active-office configuration for `dev`, `staging`, and `prod`; a dev mock Telegram mode; distinct staging test and production destinations; the NWS image-host allowlist initialized to `weather.gov` and `*.weather.gov`; and the versioned Telegram-secret JSON schema separately.
- [x] 1.3 Add shared HTTP client behavior with an identifying NWS User-Agent, JSON-compatible content negotiation, timeouts, bounded NWS collection-failure classification (`404`/non-`429` `4xx`, `429` Retry-After, `5xx`, connection failure, timeout), one permitted in-budget retry, and structured sanitized error reporting.
- [x] 1.4 Establish the public GitHub remote and repository metadata/documentation: explicit open-source license, README/service documentation, `CONTRIBUTING.md`, `SECURITY.md`, `CODE_OF_CONDUCT.md`, `CODEOWNERS`, support path, issue templates, pull-request template, labels, and public-safe operational guidance.
- [x] 1.5 Create a baseline least-privilege GitHub Actions pull-request validation workflow that runs the currently supported formatter, static checks, unit tests, lockfile validation, and pytest-cov with a 75% line-coverage floor; retain Cobertura and JSON reports for tooling consumption without a pull-request coverage summary, and make later SAM, Infracost, SBOM, and security checks additive required gates as their supporting tooling is introduced.
- [x] 1.6 After the documented initial repository-bootstrap push, configure a protected default-branch ruleset requiring squash-only pull requests, the baseline required status checks, authorized review, stale-review handling, no direct pushes for contributors other than `@hamdrew`, and stricter ownership/review for workflow, IAM, OIDC, secret, deployment, and release-policy changes; grant only `@hamdrew` an audited always-bypass for self-approval, self-merge, and occasional direct default-branch pushes.
- [x] 1.7 Verify the default-branch ruleset, CODEOWNERS coverage, baseline required checks, workflow-file review requirements, and public issue/PR template behavior; confirm direct changes to the default branch are rejected after bootstrap for every contributor except the documented `@hamdrew` sole-maintainer bypass.
- [x] 1.8 Enable and configure Dependabot alerts/security updates and update pull requests for Python/uv manifests, lockfiles, GitHub Actions, and supported package ecosystems; group only approved low-risk updates and preserve `uv sync --locked` validation.
- [x] 1.9 Enable secret scanning/push protection, CodeQL or equivalent code scanning, dependency review, license checks, and documented vulnerability-reporting/credential-rotation procedures; verify public issue/discussion templates prohibit secrets and sensitive operational logs.
- [x] 1.10 Test the currently configured CodeQL, dependency/license review, secret-scanning/push-protection, redaction, and vulnerability-reporting controls, including public-template guidance that excludes credentials and sensitive operational identifiers.
- [x] 1.11 Test baseline pull-request workflows on application, dependency, workflow, and documentation changes, including fork behavior, least-privilege permissions, pinned Actions, concurrency, and the currently applicable validation and security checks.

## 2. Story ingestion and durable history

- [x] 2.1 Implement dedicated Pydantic source models for supported flat NWS office/region JSON-LD and Weather Story response shapes, then implement all-office registry seeding and validation through `/offices/{officeId}`, geocode returned addresses into stored latitude/longitude, derive timezones from those coordinates, and provide office-agnostic retrieval for the active office supplied to an invocation; validate collection envelopes before any item work, validate items independently, quarantine malformed items while continuing valid items, and derive canonical identity from `(office_id, UUID in download URL)` only for valid items.
- [x] 2.2 Replace immutable discovery snapshots and the derived projection with one conditionally updated `STORY#` current-state record keyed by canonical `(office_id, source_story_id)`, retaining first-seen time, current revision hash/source fields/expiration, current image metadata, Telegram channel/message reference, and latest publication status. Preserve one conditionally mutable `OFFICE#{office_id}/CURRENT` record containing current NWS-enriched office/region fields, active configuration, and managed pinned-message/invite references; do not retain office metadata audit or snapshot records. Preserve append-only publication-attempt and transition-event records; bounded immutable `QUARANTINE#` records keyed by run/array index; immutable single-office run results with exactly one office ID, collection outcome, elapsed time, `required_work_completed`, bounded failure reasons, per-office counts and aggregate invocation counts for discovered/published/edited/skipped/deferred/quarantined/rejected/ambiguous outcomes, controlled deferrals, and only `success`/`success_with_deferred`/`success_with_quarantined_items`/`failed` statuses; and conditionally updated alert-fingerprint state with cooldown and dispatch outcomes. Preserve first-seen and operational state while replacing superseded source content and image metadata; do not retain a source-revision archive.
- [x] 2.3 Replace per-revision image retention with secure two-phase current-image retention: HTTPS/image-host allowlist and three-redirect policy, 20-second streaming download with a 9 MiB cap, JPEG/PNG declared-type and magic-byte validation, non-animated defensive decode, 25-megapixel/dimension-sum/aspect-ratio limits, streaming SHA-256, pending current-record metadata, deterministic staging upload, verified content-type/size/checksum/dimension validation, promotion to a deterministic current-image key, conditional committed current reference, publication gating, replacement cleanup, and indefinite retention after story expiration.
- [x] 2.4 Implement staging-object reconciliation/cleanup for uncommitted `staging/` objects, with 7-day lifecycle expiration as a secondary safety net and no committed reference to the staging prefix.
- [x] 2.5 Implement conditional create/edit reservations with leases, attempt/run IDs, target revision/message references, append-only state-transition events, sanitized response/error metadata, and the publication-attempt states `reserved`, `send_started`, `published`, `rejected`, `ambiguous`, `confirmed_received`, and `confirmed_not_received`.
- [ ] 2.6 Implement the protected operator reconciliation Lambda invoked from the AWS console or CLI, including conditional transitions, audit identity/reason, and retry eligibility after `confirmed_not_received`.
- [ ] 2.7 Add query helpers and operational documentation for reviewing current story state and lightweight first-seen/outcome facts, current images, 30-day TTL operational records, collection outcomes, elapsed time, outcome counts, run statuses, deferred outcomes, leases, sanitized response/error metadata, reconciliation actions, current-image replacement and indefinite-expiration retention boundaries, authorized manual purge, DynamoDB PITR/on-demand backup, and recovery procedures.

## 3. Telegram publishing workflow

- [ ] 3.1 Implement per-office Telegram publication of each Weather Story as exactly one retained-image photo message with its title/story-text caption and an optional `Image description: {altText}` section only when the complete section fits; include no source link, office/timestamp fields, companion or text-only message; and edit that existing photo message in place for material revisions. Revalidate retained SHA-256/type/magic bytes, byte, animation, decoded-pixel, dimension-sum, and aspect-ratio limits before every send or photo edit; reject invalid media before making a Telegram API call.
- [ ] 3.2 Implement source `endTime` expiration handling, retaining the current-story record, committed image, and publication history indefinitely, and neither deleting nor unpublishing Telegram messages.
- [ ] 3.3 Implement deterministic caption rendering as bold title plus newline and plain-text body, optionally followed by two newlines and `Image description: {altText}` only when the complete section fits; omit source links, office identifiers/display names, and issued/updated timestamp fields. Use explicit Telegram entities (no MarkdownV2/HTML/parse mode), then enforce the 1024-character post-entity-parsing limit by omitting an overlong optional description and truncating the remaining title/body at Unicode grapheme-cluster boundaries, appending `…` with final UTF-16 entity offsets.
- [ ] 3.4 Implement the one-Telegram-call-per-reservation rule, revision-edit attempt tracking, Telegram outcome classification, Retry-After handling, bounded `5xx` exponential retries through new reservations, retry/defer decisions, successful-delivery persistence, and ambiguous-outcome monitoring.
- [ ] 3.5 Implement an office-scoped scheduled handler that accepts exactly one active office ID; use a 14-minute processing deadline and 60-second shutdown reserve inside the 15-minute Lambda timeout; apply NWS, image, S3-verification, and Telegram request deadlines; retrieve and validate that office collection before selection; process at most 25 eligible valid revisions ordered by priority then order; durably persist the objective run contract with per-office and aggregate counts, elapsed time, `required_work_completed`, bounded failure reasons, and status; classify empty collections as successful, controlled unstarted work as `success_with_deferred`, valid collections with malformed siblings as `success_with_quarantined_items`, and unresolved selected rejected/ambiguous/image-invalid work as `failed`; return normally after persisting a failed run unless persistence itself fails; and surface office-collection, item-quarantine, and Telegram delivery failures.
- [ ] 3.6 Implement the protected on-demand office-info Lambda: query NWS office/region data; create or reuse the channel invite link; render the explicit-entity pinned office-information message with required and optional rows in the defined order while excluding geocoded coordinates; create or edit the one managed message and pin it; conditionally update `OFFICE#{office_id}/CURRENT` message/invite references and refreshed metadata without creating office audit/snapshot records; make all dev Telegram operations mock-only; alert and leave the office schedule disabled on required-data, Telegram-management, or pin-verification failure; and never create a Weather Story publication attempt.

## 4. Alerting and observability

- [ ] 4.1 Implement a private Telegram alert dispatcher that renders severity, fingerprint, workflow, failure summary, affected office ID, event time, and available run ID/status/elapsed-time/outcome-count context within the 3,500-grapheme-cluster limit.
- [ ] 4.2 Implement DynamoDB-backed versioned alert fingerprints, atomic state updates, four-hour cooldown suppression, and ongoing aggregation; distinguish `critical`, `error`, and metric-only `warning` severities.
- [ ] 4.3 Implement separate SNS alert-trigger and fallback topics; route CloudWatch state-transition alarms and application failures to the trigger topic, and configured email notification to the fallback topic when private Telegram alert delivery fails.
- [ ] 4.4 Implement one structured-log sanitizer and allowlisted event schema for publisher, alert-dispatcher, reconciliation, and rotation-smoke Lambda logs. Define validated non-secret environment-scoped `DEBUG`/`INFO`/`WARNING`/`ERROR` log levels, defaulting dev/staging to `DEBUG` and prod to `INFO` while rejecting prod `DEBUG`. Emit only event timestamp, level, type, component, safe operational IDs (`office_id`, `run_id`, `attempt_id`, revision hash), outcome/status, HTTP status, stable error class/code, a bounded sanitizer-produced error summary, latency, retry data, and aggregate counts for single-office run summaries/statuses, NWS retrieval, image retention, deduplication decisions, cross-office source-UUID collision warnings, publication outcomes, alert-dispatch outcomes, suppressed/aggregated alerts, and fallback delivery. At DEBUG, add only allowlisted request-boundary/latency, validation-stage/code, retry-budget, and deduplication/reservation decision events. Omit raw/token-bearing URLs, Telegram chat/message IDs, story IDs, S3 keys, story/image content or metadata, secrets, headers, request/response bodies, raw exceptions/stack traces, and unbounded upstream errors; use durable history for authorized raw-reference lookup.
- [ ] 4.5 Configure SNS-to-Lambda subscription permissions, retry behavior, filtering, and loop prevention so upstream errors, Telegram failures, and ambiguous delivery outcomes reach the alert workflow without posting alerts to the public channel; prohibit alert-dispatcher and fallback failures from returning to the alert-trigger topic or Telegram.

## 5. AWS SAM infrastructure

- [ ] 5.0 Treat `infracost-integration` tasks 1.1–5.6 as a deployment prerequisite: SAM template authoring, local validation/build, and non-mutating estimation may proceed, but no task or procedure may create, update, or delete an AWS application resource until the required Infracost gate has a current pass or documented authorized override for the exact template revision and target environment.
- [ ] 5.1 Create the AWS SAM template, parameters, mappings, and outputs for Lambda packaging and non-secret runtime configuration, including Python 3.13/arm64 for every Lambda, an explicit 900-second publisher timeout, and 1024 MB publisher memory setting. Deploy only to `us-east-2` in the single authorized AWS account; support only `dev`, `staging`, and `prod`; derive environment-tagged stack/resource names; apply mandatory `Application`, `Environment`, and `Owner` tags with the owner set to Andrew Hoffmann (`@hamdrew`); and validate non-secret environment, exact environment-specific pre-created Telegram-secret ARN, deployment mode, active-office, and log-level parameters. Default dev/staging log level to `DEBUG`, prod to `INFO`, and reject `DEBUG` for prod. Reject empty, malformed, cross-environment, and secret-value parameters, and never emit secrets in outputs or change sets.
- [ ] 5.1a Build reproducible ZIP artifacts with `sam build --use-container` and a pinned digest for the Python 3.13 arm64 SAM build image; use a pinned `uv` executable and `uv sync --locked` from the committed `uv.lock`; package pinned Pillow wheels and verify native imports/decoding under the matching build image; generate an artifact/dependency SBOM; run blocking vulnerability and license scans; retain scan/SBOM evidence with the release and document reviewed exception handling.
- [ ] 5.2 Provision DynamoDB with explicit `OFFICE#`, `STORY#`, `ATTEMPT#`, `TRANSITION#`, `RUN#`, `QUARANTINE#`, and `ALERT#` key families; use only conditionally mutable `OFFICE#{office_id}/CURRENT` records and no office audit/snapshot records; add `office-current-index` with `office_current_pk` and `office_current_sk` to query current stories by source expiration time without a table scan; enable one DynamoDB TTL attribute, set 30 days after creation or latest update on attempts, transitions, runs, quarantine, and alert state while omitting it from `OFFICE#` and `STORY#` records; enable 35-day PITR, monthly AWS Backup snapshots retained one year, and an operator-triggered pre-destructive on-demand backup procedure; a private SSE-S3 image bucket with all Block Public Access controls, `BucketOwnerEnforced`, TLS-only bucket policy, versioning without cross-Region replication, no public outputs/ACLs, current-image retention after story expiration, 30-day noncurrent-version expiration, and 7-day staging expiration; and separate publisher, alert-dispatcher, reconciliation, rotation-smoke, office-info, Scheduler-execution, and documented pre-created deployment roles. Implement exact-resource IAM action matrices: publisher access only to story/attempt/transition/run/quarantine keys (with `dynamodb:LeadingKeys` where supported), `staging/`/current-image object operations and staging/current-image deletion, the `AWSCURRENT` secret, and alert-trigger SNS; office-info access only to `OFFICE#{office_id}/CURRENT`, the exact `AWSCURRENT` secret, NWS office/region endpoints, and required Telegram chat/invite/message/pin operations; dispatcher access only to `ALERT#` records, the `AWSCURRENT` secret, and fallback SNS; reconciliation access only to attempts/transitions; rotation smoke access only to the `AWSPENDING` secret and `getMe`; Scheduler invoke only the qualified publisher ARN with source-account/source-ARN conditions; deployment role pass only named roles and manage only application resources. Deny `Scan`, secret listing/write/rotation, unrelated secrets/buckets/topics/functions, runtime role assumption, creation of office audit/snapshot records, and runtime deletion of retained office/story records. Apply scoped customer-managed-key decrypt when applicable and provision publisher/alert-dispatcher/reconciliation/rotation-smoke/office-info log groups with 90-day retention.
- [ ] 5.3 Provision one SAM `ScheduleV2`/EventBridge Scheduler schedule per active office, each with a configurable UTC `rate(15 minutes)` rate, `FlexibleTimeWindow: OFF`, explicit execution role, `MaximumRetryAttempts: 0`, maximum event age of 60 seconds, the publisher Lambda target, and an input containing exactly that office ID; create only the MKX schedule for MVP. Create or update schedules disabled, then enable only through the documented environment-specific smoke gate.
- [ ] 5.4 Provision `WeatherStoryBot` metrics with only `Environment` and `OfficeId` dimensions: `RunStarted`, `RunSucceeded`, `RunFailed`, `OfficeRetrievalFailed`, `StoriesDiscovered`, `StoriesPublished`, `StoriesEdited`, `ReservationsAmbiguous`, `ImageUploadFailed`, `Telegram429`, `AlertFallbackUsed` as `Count`; `RunDurationMs`, `NwsRequestDurationMs`, `ImageDownloadDurationMs`, and `TelegramRequestDurationMs` as `Milliseconds`; and existing publisher duration/max-memory, quarantine, deferred, alert-dispatch, and fallback signals. Provision one environment dashboard with run health/volume, failure/ambiguity/fallback, p50/p90/p99 latency, resource use, and schedule state. For enabled schedules configure missing `RunStarted` at 20 minutes, `RunFailed`/ambiguity at one in 15 minutes, retrieval/image failures at two across consecutive 15-minute windows, Telegram 429 in both of two consecutive 15-minute windows, and fallback at one in 15 minutes; disable alarm actions while schedules are intentionally disabled and enable them with the smoke-gated schedule step. Use metric/composite-alarm state-transition actions targeting the alert-trigger SNS topic; configure the alert-dispatch Lambda subscription/resource policy, the separate fallback SNS/email subscription, and retry/error handling without SQS.
- [ ] 5.5 Document secure secret setup, the JSON schema, per-role exact-ARN/action/condition IAM matrix and environment-scoped deployment-role trust boundary, 60-second cache behavior, BotFather-to-`AWSPENDING`-to-`AWSCURRENT` rotation and rollback procedure using the dedicated protected `getMe` smoke Lambda, runtime smoke tests, and SAM deployment. Document `us-east-2`/single-account governance, mandatory tags and Andrew Hoffmann (`@hamdrew`) ownership, the $100 monthly budget with 80% forecast/100% actual/120% escalation notifications, change-set review for staging/prod, required human prod approval, monthly and pre-prod drift detection with promotion blocked on unresolved drift, normal CloudFormation rollback and retained stateful resources, environment isolation, dev mock-only Telegram delivery, staging dedicated real-channel smoke delivery, prod non-publishing pre-enable checks, safe scheduler enablement, and the response to a partial external send (disable schedules; inspect durable history; reconcile/recover; validate before re-enable). Also document email subscription confirmation, recovery of an ambiguous publication reservation, authorized permanent-purge controls, pre-destructive DynamoDB backup, and same-Region restore/cutover/rollback runbook.
- [ ] 5.5a Document the on-demand office-info invocation, NWS office/region enrichment, conditionally mutable `OFFICE#{office_id}/CURRENT` and office-info-role boundaries, invite-link creation/reuse, pinned-message format and update/pin verification, dev mock-only behavior, staging/prod channel checks, required-data/Telegram-management failure handling, absence of office audit/snapshot records, and the rule that invite links and tokens never appear in outputs or logs.

## 6. Verification

- [ ] 6.1 Maintain an explicit, versioned, sanitized NWS source contract plus captured success/error fixtures for flat JSON-LD office responses, referenced regional-office responses, and Weather Story collections; contract-test all-office seed loading, office lookup seeding, address geocoding, coordinate-to-timezone lookup, registry validation, inactive-without-channel and active-channel requirements, per-active-office channel uniqueness, complete/empty `stories[]` envelopes, unsupported pagination, missing required fields, unknown fields, valid-sibling continuation and quarantine, canonical `(office_id, source_story_id)` identity, cross-office source-UUID collision warnings, revision hashing, unchanged/changed/reused-identity stories, current-state replacement with preserved first-seen/publication state, end-time expiration without story/image deletion, omission handling, `404`/`429`/`5xx`/connection/timeout classification and in-budget retry decisions, image-host/HTTPS/redirect policy, streaming 9 MiB limit and partial-download handling, MIME/magic-byte agreement, JPEG/PNG acceptance, non-animation/decompression-bomb defenses, decoded-pixel/dimension/ratio limits, SHA-256 verification, current-image retrieval/retention, promotion from staging to current key, two-phase image commit, replacement cleanup, and malformed/upstream-error paths. Add documented, explicitly opt-in, read-only live NWS contract tests for representative office, regional-office, and Weather Story collection endpoints using bounded timeouts and the identifying User-Agent; keep them out of default unit/integration and pull-request commands and require no credentials or retained-image downloads.
- [ ] 6.2 Add unit tests for lease expiry, conditional reservation, concurrent reservation races, attempt/run ID assignment, append-only transition and single-office run records with per-office/aggregate counts, elapsed time, `required_work_completed`, status, and bounded failure reasons; sanitized response/error metadata; every permitted state transition; one Telegram API call per reservation; deterministic Telegram `429` Retry-After, `5xx`, timeout, accepted-then-lost-response, 4xx/non-retryable, retry/defer, and ambiguity classification; run-budget and story-cap deferrals; overlapping attempts; and failed-run persistence with normal handler return after persistence.
- [ ] 6.3 Add unit tests for reconciliation authorization, idempotence, append-only reconciliation audit events, and retry eligibility after `confirmed_not_received`.
- [ ] 6.4 Add unit tests for single-photo-message publishing; optional fitting/omitted `Image description: {altText}` behavior; no source link/office/timestamp fields; retained-image pre-send SHA-256/MIME/magic-byte/byte/animation/pixel/dimension/ratio revalidation and no Telegram call for invalid media; bold-title/newline/plain-text caption entities; literal formatting-like characters; 1024-character post-entity-parsing caption boundaries; grapheme-safe truncation for emoji, ZWJ sequences, combining marks, CJK, and RTL text; valid UTF-16 entity offsets; initial channel publishing; in-place photo/caption revision edits; deterministic Telegram 429/5xx/timeout/accepted-then-lost faults; edit failure/ambiguity; no-companion-message behavior; private alert-message formatting without exposing secrets; secret JSON-schema validation; explicit `AWSCURRENT` retrieval; 60-second cache expiry; dedicated `AWSPENDING` rotation-smoke `getMe` path with no publishing/data-store permissions; `AWSPREVIOUS` rollback; IAM boundary validation; token/token-bearing-URL redaction; structured-log allowlist enforcement; dev/staging DEBUG default event coverage; prod DEBUG rejection; DEBUG omission of sensitive IDs/content and raw exception data; bounded sanitized error summaries; durable-history correlation through safe IDs; and 90-day log-retention configuration.
- [ ] 6.4a Add office-info Lambda tests for NWS office/region enrichment, required/optional field omission, explicit-entity formatting and link order, invite-link creation/reuse, idempotent managed-message create/edit/pin behavior, conditionally updated `OFFICE#{office_id}/CURRENT` references with no office audit/snapshot creation, no Story publication-attempt creation, dev mock-only behavior, and schedule-disabled-on-failure handling.
- [ ] 6.5 Add unit and integration tests for exact `WeatherStoryBot` metric names/units and `Environment`/`OfficeId` dimensions; zero-story success versus missing `RunStarted`; latency metric percentiles; alarm thresholds and enabled-schedule gating; dashboard widgets and schedule state; CloudWatch state-transition/application failure publication to the alert-trigger SNS topic; fingerprint construction; concurrent duplicate handling; four-hour suppression while metrics continue; ongoing aggregation; severity; 3,500-grapheme alert truncation; SNS-to-Lambda delivery; fallback SNS/email behavior; and alert-path loop prevention.
- [ ] 6.6 Add integration-style tests using mocked NWS, Telegram, S3, DynamoDB, and SNS clients to verify one MKX story is retained, a revised story edits the existing Telegram message/photo, expiration does not delete history, reprocessing produces no duplicate, S3 partial uploads/orphans are not publishable and are reconciled, retained objects are retrieved and revalidated, deterministic Telegram fault injection follows the retry/ambiguity contract, and failures alert the private recipient/fallback without loops.
- [ ] 6.7 Add tests verifying environment isolation for dev/staging/prod stack and resource names/tags, exact environment-only secret ARN/configuration, no cross-environment references, dev mock-only Telegram delivery with no message API call, staging dedicated real-test-channel delivery, and production-only production-channel configuration; parameter validation/rejection; change-set and human-production-approval gates; monthly/pre-prod drift checks blocking promotion; normal CloudFormation rollback with retained stateful resources; schedules disabled after create/update and only enabled after the appropriate smoke gate; and partial-external-send rollback handling. Also verify an independent Scheduler/Lambda invocation and office-ID payload for every active office, no schedule for inactive offices, MKX-only MVP scheduling, Scheduler retry settings, Scheduler source-account/source-ARN trust conditions, and per-role IAM allow/deny boundaries for DynamoDB key families, S3 prefixes/staging-only deletion, exact secret version stages, SNS topics, qualified publisher invocation, and deployment-role pass-role scope; the 900-second timeout, 1024 MB memory setting, 14-minute processing deadline, 60-second reserve, request deadlines, and 25-revision cap; priority/order selection; exact per-office and aggregate run-result fields; empty-collection success with zero discovered and `required_work_completed=true`; `success_with_deferred`/`success_with_quarantined_items` versus failed classification; unresolved rejected/ambiguous/image-invalid work making the persisted run failed while the handler returns normally after persistence; collection/persistence failure versus item-quarantine/Telegram-failure return semantics and alerting; stale `send_started` recovery after interruption; and that failed or ambiguous invocation outcomes rely on the next invocation for that office plus DynamoDB state rather than an automatic duplicate invocation.
- [ ] 6.8 Run formatting, static checks, unit/integration tests, Python 3.13/arm64 compatibility tests, `uv sync --locked` enforcement, representative Pillow import/decode tests in the pinned SAM build container, SBOM generation, vulnerability/license scans, `sam validate`, metrics/dashboard/alarm verification, and objective smoke tests. Capture smoke acceptance evidence for the `us-east-2` single-account deployment, mandatory tags/owner, $100 budget notifications, dev mock-only delivery, staging dedicated-channel photo delivery, prod non-publishing checks, persisted run status/counts/elapsed time/`required_work_completed`, no unresolved required work, expected metrics, no unexpected alert/fallback, and smoke-gated scheduler/alarm enablement; also capture manual deployment steps for change-set review, prod approval, drift review, retained-resource rollback, environment isolation, 15-minute scheduling, private/TLS-only image-bucket controls, lifecycle transitions and staging cleanup, image retention, truncation, operator reconciliation, operator alerts, and intentionally disabled-schedule alarm gating.
- [ ] 6.8a Run the on-demand office-info smoke test in staging/prod: query current NWS office/region data, create/reuse the invite link, create or edit and pin exactly one formatted message, verify the durable references and no duplicate, and leave the office schedule disabled if any required step fails; run the equivalent dev path with mocked Telegram operations only.
- [ ] 6.9 Perform and record a quarterly same-Region recovery exercise: restore DynamoDB PITR or monthly backup to a new isolated table; reapply IAM, tags, TTL, PITR, alarms, and required runtime configuration; verify sampled immutable history and retained/noncurrent S3 image version checksums; demonstrate no production schedule runs against the restored table before controlled cutover; and remove the exercise resources. Rehearse ambiguous-publication reconciliation for both `confirmed_received` and `confirmed_not_received`, verify no duplicate publication, and record durable state, alerts, and rollback evidence.
- [ ] 6.10 After satisfying task 5.0 for the exact CI template revision and `dev` environment, run an ephemeral uniquely named dev AWS control-plane verification stack in CI: build with the pinned SAM container; create/review a change set; create/update the stack; invoke the deployed Scheduler target and verify retry/maximum-event-age behavior; exercise S3 staging upload, orphan cleanup, retained-object retrieval; confirm SNS subscription and fallback delivery; exercise CloudFormation rollback and drift detection; collect bounded evidence; and destroy the stack without publishing to Telegram.

## 7. GitHub-managed deployment and release

- [ ] 7.1 Configure GitHub OIDC trust and environment-scoped AWS deployment roles for `dev`, `staging`, and `prod`; restrict repository/workflow/ref/environment claims, prohibit long-lived AWS keys, isolate environment variables/secrets, and require human approval for production. Document the temporary audited workstation bootstrap path for dev mock-only and staging dedicated-channel smoke deployments, requiring the exact-revision/environment Infracost pass or documented authorized override before any AWS apply path, and explicitly prohibit workstation production deployments.
- [ ] 7.2 Extend GitHub Actions with separate Infracost/security-evidence, deployment, and release-publication workflows. Run formatting, static checks, unit/integration tests, SAM validation/build, dependency scans, SBOM generation, and artifact retention with least-privilege permissions; pin every GitHub Action to the full commit hash of its latest release with an inline release-tag comment, define safe concurrency/cancellation rules, restrict workflow token permissions, redact secrets from logs/artifacts/comments, and add workflow-policy tests for changed-file coverage and fork behavior.
- [ ] 7.3 Implement the release workflow with protected semantic-version tags, generated release notes/changelog entries, signed tags and/or verifiable build provenance, packaged Lambda artifacts, SBOMs, scan results, Infracost results, source/lock/template digests, and CloudFormation change-set/deployment traceability.
- [ ] 7.4 Document release promotion, rollback, compromised-workflow response, secret rotation, artifact verification, and the distinction between selecting a prior artifact and reversing external Telegram effects.
- [ ] 7.5 Test full security and release surfaces, including CodeQL/dependency/license/secret scanning, push protection, redaction, vulnerability-reporting guidance, and rejection of credentials or sensitive identifiers in workflow logs, artifacts, comments, and release assets.
- [ ] 7.6 Test extended pull-request workflows on application, infrastructure, dependency, workflow, and documentation changes, including fork behavior, least-privilege permissions, pinned Actions, concurrency, and the applicable required validation, Infracost, SAM, and security checks.

## 8. GitHub deployment and release verification

- [ ] 8.1 Test OIDC claims and IAM trust boundaries for approved and unapproved repositories, refs, workflows, and environments; verify dev/staging/prod isolation, the audited dev/staging workstation-bootstrap boundary, prohibition of workstation prod deployment, and the production approval gate.
- [ ] 8.2 Test release creation, protected tags, generated notes, signature/provenance verification, SBOM and scan attachment, source/dependency/template traceability, failed-release blocking, and prior-artifact rollback evidence.
